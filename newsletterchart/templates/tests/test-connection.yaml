apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "newsletterchart.fullname" . }}-test-connection"
  labels:
    {{- include "newsletterchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "newsletterchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
